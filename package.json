{
  "devDependencies": {
    "standard": "^17.1.0",
    "vite": "^4.4.5"
  },
  "eslintConfig": {
    "extends": [
      "standard"
    ]
  },
  "name": "tetris-rush",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "start": "serve dist -s -n -L -p $PORT"
  },
  "type": "module",
  "version": "0.0.0",
  "dependencies": {
    "serve": "^14.2.1"
  }
}
